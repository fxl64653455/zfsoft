server:
  tomcat:
    max-threads: 100
    min-spare-threads: 20
  connection-timeout: 5000
  ssl:
    key-store: classpath:.keystore
    key-store-type: JKS
    key-password: qq123456
    key-alias: tomcat
  port: 8080
spring:  
  redis:
    database: 1
    host: 121.43.110.87
    pool:
      max-active: 20
 # 数据源配置：
  datasource:
    name: 
    driver-class-name: oracle.jdbc.OracleDriver
    #oracle.jdbc.driver.OracleDriver
    #type: com.zfsoft.boot.hikaricp.ds.NewHikariDataSource
    #url: Kbs2u6NELkMD+i6RnR+aSRYguMAm9Sijs60p2ZfE8+wHkVsh0SdX85+Y60A4RnMl
    #username: 8J02YYS4gwOdDrhW0kL0ZQ==
    #password: 8J02YYS4gwOdDrhW0kL0ZQ==
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:oracle:thin:@10.71.19.135:1521:orcl
    username: 
    password: 
    # HiKariCP的数据源配置：
    hikari: 
      connection-timeout: 30000 #等待连接池分配连接的最大时长（毫秒），超过这个时长还没可用的连接则发生SQLException， 缺省:30秒
      idle-timeout: 600000 #一个连接idle状态的最大时长（毫秒），超时则被释放（retired），缺省:10分钟 
      maxLifetime: 1800000 #一个连接的生命时长（毫秒），超时而且没被使用则被释放（retired），缺省:30分钟，建议设置比数据库超时时长少30秒以上，参考MySQL wait_timeout参数（show variables like '%timeout%';）
      maximum-pool-size: 15 #连接池中允许的最大连接数。缺省值：10；推荐的公式：((core_count * 2) + effective_spindle_count) 
      connection-test-query: select 1 from dual

sharding.jdbc.config:
  sharding: 
    enabled : false
    dataSources:
      ds: 
        driverClassName: com.zaxxer.hikari.HikariDataSource
        url: jdbc:oracle:thin:@10.71.19.135:1521:orcl
        username: ZFTAL_METRICS
        password: ZFTAL_METRICS
        connection-timeout: 30000 #等待连接池分配连接的最大时长（毫秒），超过这个时长还没可用的连接则发生SQLException， 缺省:30秒
        idle-timeout: 600000 #一个连接idle状态的最大时长（毫秒），超时则被释放（retired），缺省:10分钟 
        maxLifetime: 1800000 #一个连接的生命时长（毫秒），超时而且没被使用则被释放（retired），缺省:30分钟，建议设置比数据库超时时长少30秒以上，参考MySQL wait_timeout参数（show variables like '%timeout%';）
        maximum-pool-size: 15 #连接池中允许的最大连接数。缺省值：10；推荐的公式：((core_count * 2) + effective_spindle_count) 
        connection-test-query: select 1 from dual
        
    shardingRule:
      bindingTables:
        - ZFTAL_METRIC_DATA
      #默认数据库分片策略
      defaultDatabaseStrategy:
        none:
      defaultTableStrategy:
        complex:
          shardingColumns: TIME24
          algorithmClassName: com.zfsoft.boot.metrics.setup.sharding.ShardingAlgorithm
      props:
        sql.show: true     